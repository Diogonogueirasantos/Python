from typing import List

usuario = List[int]

class search_binarie():

    def cadastros(self, usuario):
        for c in range(0, 100):
            usuario.append(c)
    

    def unico(self):
        repr(usuario)


x = search_binarie

print(x.unico())